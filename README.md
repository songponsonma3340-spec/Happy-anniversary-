# เซอร์ไพรส์ครบรอบ 2 ปี 1 เดือน (Quiz → YouTube)
เว็บเพจเล็ก ๆ สำหรับให้ตอบคำถามก่อน แล้วค่อยปล่อยวิดีโอ YouTube (จากลิงก์ Shorts: `Dytw4JlyFec`).

## โครงสร้างไฟล์
```
anniversary-quiz/
├── index.html
├── style.css
└── script.js
```

## วิธีใช้งานบน GitHub Pages
1. สร้าง repository ใหม่ชื่ออะไรก็ได้ (เช่น `anniversary-quiz`).
2. อัปโหลดไฟล์ทั้งสาม (`index.html`, `style.css`, `script.js`) ไปที่สาขา `main` (หรือ `master`).
3. ไปที่ **Settings → Pages** เลือก **Branch: main** และ **/ (root)** แล้วกด Save.
4. รอสักครู่จะได้ URL ประมาณ `https://<username>.github.io/<repo>/` เอาลิงก์นี้ไปส่งให้คนพิเศษได้เลย 💖

> **หมายเหตุเรื่อง autoplay**  
> มือถือส่วนใหญ่ไม่อนุญาตให้วิดีโอเล่นอัตโนมัติพร้อมเสียง เราจึงทำให้เริ่มแบบ *muted* และแสดงปุ่ม **เปิดเสียง** ให้กดเอง เพื่อให้แน่ใจว่าวิดีโอเล่นได้เสมอ

## ปรับแต่ง
- เปลี่ยนตัวเลือก/ข้อความ ได้ที่ `index.html` และเงื่อนไขคำตอบถูก (`CORRECT_PREFIX`) ใน `script.js`.
- เปลี่ยนวิดีโอ: แก้ `VIDEO_ID` ใน `script.js` เป็นรหัสวิดีโอ YouTube ที่ต้องการ
- เพิ่มเอฟเฟกต์/คอนเฟตติ สามารถปรับพารามิเตอร์ใน `launchConfetti()`

ขอให้มีความสุขกับวันครบรอบนะครับ 🎉
